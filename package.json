{
  "name": "hexagonal-bokeh-blur",
  "version": "1.0.0",
  "description": "",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "parcel build src/index.html --public-url=/hexagonal-bokeh-blur/dist/",
    "serve": "live-server --port=1234 ./dist/",
    "watch": "parcel watch ./src/index.html --no-hmr",
    "start": "npm-run-all --parallel watch serve"
  },
  "author": "Robert Leitl",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.0.0",
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3",
    "tweakpane": "^3.0.6",
    "worley-noise": "^2.0.1"
  }
}
